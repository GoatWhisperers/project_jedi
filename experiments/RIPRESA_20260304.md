# RIPRESA — 2026-03-04

> Leggi prima STATO.md, poi questo file.

---

## Stato al termine sessione 2026-03-03

### Batch in background — LASCIATO GIRARE
- Script: `/tmp/run_gemma2_ripresa3.sh` (PID 57948 al termine sessione)
- Decompose: `decompose.py` — Gemma2-Uncensored, tutti 9 concept
- Log: `/tmp/gemma2_ripresa3.log` e `/tmp/decompose_gemma2_ripresa3_20260303_181859/`
- Stato al logout: **1/9 concept in corso** — hot_vs_cold step 5 (steering eval)

### Vettori Gemma2 Gd1 estratti (verificati)
- **hot_vs_cold**: 4 sub-concept × 10 layer = 80 file ✅ (temperature_intensity, sensory_reaction, pain_vs_numbness, material_type)
- restanti 8 concept: estrazione in corso in background

### Gd1 Gemma3 (già completato sessione precedente)
- 9/9 concept × 10 layer × ~6 sub-concept = 600 file ✅

---

## Fix critici fatti oggi

### 1. gpu_utils.py (NUOVO — `scripts/gpu_utils.py`)
Modulo centrale per gestione GPU. Usato da `decompose.py` step 3.
- Prima del probe: `gpu_prepare_for_probe()` → unload + verifica VRAM libera
- Dopo il probe: `gpu_restore_after_probe()` → ricarica modello
- Bug corretto: unload usava POST invece di GET → ora usa `_get()`

### 2. steering_server.py — fix ROCm VRAM
- Aggiunto `import gc` + `gc.collect()` prima di `torch.cuda.empty_cache()`
- `State.lock.acquire(timeout=120)` invece di `with State.lock:` (non bloccante)
- Risposta 503 se lock timeout
- **EFFETTO**: VRAM si libera davvero dopo unload (da 17.4 GB → 9.3 GB confermato)

### 3. Cantagallo
- `claude_is_busy()` controlla tmux pane — se occupato: solo popup, no pending
- Messaggi trasformati da ordini a proposte

### 4. Infrastruttura sessioni
- `STATO.md` — sempre il primo file da leggere
- `update_stato.sh` — aggiorna STATO.md con dati live
- CLAUDE.md — checklist inizio/fine sessione obbligatorie

---

## Prima cosa da fare domani

```bash
# 1. Leggi STATO.md
cat STATO.md

# 2. Controlla se il batch è ancora vivo o terminato
pgrep -f "run_gemma2_ripresa3.sh" && echo "ANCORA IN CORSO" || echo "TERMINATO"
tail -30 /tmp/gemma2_ripresa3.log

# 3. Controlla vettori estratti
find output/vector_library -path "*/sub/*/gemma2-uncensored/layer_*.npy" ! -name "*_pca*" | wc -l
# Atteso se batch completo: ~9 concept × ~4 sub × 10 layer = ~360 file

# 4. Se batch completato → build catalog e commit
project_jedi/.venv/bin/python project_jedi/scripts/build_catalog_multi.py
cd project_jedi && git add -A && git commit -m "data: Gd1 Gemma2-Uncensored completo — 9/9 concept"

# 5. Se batch fermo/fallito → guarda logs e rilancia
# Script ripresa da usare: scripts/run_decompose_gd1_all.sh o nuovo ripresa4
```

---

## Prossimi passi (dopo Gd1 completo)

1. Scrivere `experiments/07_gemma2_decompose_gd1.md` — analisi comparativa Gemma3 vs Gemma2 Gd1
2. Avviare ricerche riservate (vedi `/home/lele/ricerche_riservate/filone_ricerca.txt`)
3. Aggiornare MEMORY.md con stato Gd1 Gemma2 completato

---

## Geometria hot_vs_cold Gemma2 (dati ripresa3, verificati)

```
                          hot temper sensor   pain materi
hot_vs_cold               1.000  0.422  0.100  0.148 -0.309
temperature_intensity     0.422  1.000  0.314  0.278 -0.215
sensory_reaction          0.100  0.314  1.000  0.322  0.030
pain_vs_numbness          0.148  0.278  0.322  1.000  0.024
material_type            -0.309 -0.215  0.030  0.024  1.000
```

- `material_type` è **anticorrelato** con `hot_vs_cold` (-0.309) → cattura il materiale, non la temperatura
- `temperature_intensity` è il più correlato al broad (0.422) → coerente con Gemma3
- Sub-concept best layer: temperature_intensity→L38, sensory_reaction→L38, pain_vs_numbness→L29, material_type→L32
